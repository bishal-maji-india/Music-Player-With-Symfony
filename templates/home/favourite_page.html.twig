<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Favourities</title>
    {# the CSS file lives at "public/css/styles.css" #}
    <link href="{{ asset('css/styles.css') }}" rel="stylesheet"/>
</head>
<body class="bg_primary">
<div class="margin-top-low">
    <table class="table">
        <thead>
        <tr>
            <th>sr no</th>
            <th>sr no</th>
            <th>sr no</th>
        </tr>
        </thead>
        <tbody>
        {% for item  in favourite_arr %}

        <tr class="flex">
            <td><img class="icon" src="http://localhost/uploads/thumbnails/{{ item.thumb }}" alt="Play"></td>
            <td><h2>{{item.name }}</h2></td>
            <td><a href=" {{ path('open_player_route', {id: item.id, name: item.name, audio:item.audio, singer:item.singer, genre:item.genre, thumb:item.thumb}) }}"><img class="icon"  src="http://localhost/uploads/icons/play_icon.png" alt="Play"></a></td>
        </tr>
        {% endfor %}

        </tbody>
    </table>

</div>

</body>



</html>